export type PackageType = {
  id: number;
  Package: string;
  status: string;
  receiver: string;
  sender: string;
  origin: string;
  destination: string;
  weight: number;
  trackingCode: string;
};
